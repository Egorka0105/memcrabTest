export enum ROUTES {
  MAIN = '/',
  TABLE = 'table',
}

export enum FIELD_NAMES {
  ROWS = 'rows',
  COLUMNS = 'columns',
  CELLS = 'cells',
}

export const INITIAL_FORM_VALUES = {
  columns: 0,
  rows: 0,
  cells: 0,
};
