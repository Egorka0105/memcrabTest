export enum ROUTES {
  MAIN = '/',
  TABLE = 'table',
}

export enum FIELD_NAMES {
  ROWS = 'rows',
  COLUMNS = 'columns',
  CELLS = 'cells',
}

export enum STORAGE_KEYS {
  FORM_VALUES = 'form-values',
}
